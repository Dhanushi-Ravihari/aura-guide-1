export const CONFIG = {
  /** Go API (auth, tasks, profile, …) */
  API_BASE_URL: "http://localhost:8080",
  /** FastAPI AI agent — must match `uvicorn` host/port; use your LAN IP on a physical device */
  AI_AGENT_BASE_URL: "http://localhost:8000",
};
